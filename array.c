#include<stdio.h>

int main()
{
   int n;
    int marks[5];

    for (int i = 0; i < 5; i++)
    {
        printf("the value of %d element of matrix is",i);
        printf("\n");
        scanf("%d",&marks[i]);
        // bracket mein likha i works as a variable, condition ke according vo kisi ka bhi roop lelega
        
    }
      
    return 0;
}
